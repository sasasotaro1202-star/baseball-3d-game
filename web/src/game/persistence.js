export const SAVE_SCHEMA_VERSION=2;
export function migrateSave(input={}){const base={version:SAVE_SCHEMA_VERSION,currency:1000,collection:[],team:[],settings:{sound:true,haptics:true},progress:{matches:0,wins:0},lineup:null};const merged={...base,...input,version:SAVE_SCHEMA_VERSION};merged.settings={...base.settings,...(input.settings||{})};merged.progress={...base.progress,...(input.progress||{})};return merged;}
